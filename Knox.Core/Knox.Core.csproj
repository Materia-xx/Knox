<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- netstandard2.0 so this library is consumable by BOTH the .NET Framework 4.7.2
         WPF app (Knox) and modern .NET (Knox.App MAUI for Windows + Android). -->
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>Knox</RootNamespace>
    <AssemblyName>Knox.Core</AssemblyName>
    <ProjectGuid>{A1B2C3D4-0000-4000-A000-000000000001}</ProjectGuid>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Azure.Security.KeyVault.Secrets" Version="4.2.0" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.1" />
  </ItemGroup>

</Project>
